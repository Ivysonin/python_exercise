# listas
    # Armazenar mais de uma informação em variáveis
    # Manter a sequencia dos dados em uma variável

cores = ['amarelo', 'vermelho', 'verde', 'azul']
valor = [10, 20, 30, 40]

duas_listas = list(zip(cores, valor))

print(duas_listas)