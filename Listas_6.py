# Listas
    # Armazenar mais de uma informação em variáveis
    # Manter a sequencia dos dados em uma variável

frutas_usuario = input('Digite o nome das frutas separados por vírgulas: ')

lista_frutas = frutas_usuario.split(', ')

print(lista_frutas)